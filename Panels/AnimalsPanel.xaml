<UserControl x:Class="Zoo_Management.Controls.Panels.AnimalsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Zoo_Management.Controls.Panels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="5"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20 0">
            <Button>
                <materialDesign:PackIcon Kind="Trash"/>
            </Button>
            
            <Button>
                <materialDesign:PackIcon Kind="Add"/>
            </Button>
        </StackPanel>

        <Separator Grid.Row="1" Background="{StaticResource Apricot}" Margin="20 0" VerticalAlignment="Center"/>

        <ScrollViewer Grid.Row="2">
            <StackPanel x:Name="AnimalsStackPanel" Orientation="Vertical" HorizontalAlignment="Center">
                
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
